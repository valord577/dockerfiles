name: ct-linux419-arm64-gnu
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/_buildx.yml'
      - '.github/workflows/ct-linux419-arm64-gnu.yml'
      - 'cross-toolchain-linux-base/.config.linux419.glibc217.arm64'
      - 'cross-toolchain-linux-base/Dockerfile'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/_buildx.yml'
      - '.github/workflows/ct-linux419-arm64-gnu.yml'
      - 'cross-toolchain-linux-base/.config.linux419.glibc217.arm64'
      - 'cross-toolchain-linux-base/Dockerfile'

jobs:
  build:
    secrets: inherit
    uses: ./.github/workflows/_buildx.yml
    with:
      build-args: |
        LINUX_UAPI_VERSION=419
      context: 'cross-toolchain-linux-basic'
      target: 'target-ct-arm64-glibc'
