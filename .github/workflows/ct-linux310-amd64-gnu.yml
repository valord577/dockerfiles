name: ct-linux310-amd64-gnu
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/_buildx.yml'
      - '.github/workflows/ct-linux310-amd64-gnu.yml'
      - 'cross-toolchain-linux-base/.config.linux310.glibc217.amd64'
      - 'cross-toolchain-linux-base/Dockerfile'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/_buildx.yml'
      - '.github/workflows/ct-linux310-amd64-gnu.yml'
      - 'cross-toolchain-linux-base/.config.linux310.glibc217.amd64'
      - 'cross-toolchain-linux-base/Dockerfile'

jobs:
  build:
    secrets: inherit
    uses: ./.github/workflows/_buildx.yml
    with:
      build-args: |
        LINUX_UAPI_VERSION=310
      context: 'cross-toolchain-linux-basic'
      target: 'target-ct-amd64-glibc'
