#!/usr/bin/env sh
set -e

DIR=$(cd "$(dirname "${0}")"; pwd)


basename="${0##*/}"
triplet="${basename%%-windres}"

llvm-windres \
  --target=${triplet} \
  --preprocessor-arg="--sysroot=$(realpath $DIR/../$triplet)" \
  $@
